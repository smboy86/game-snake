# Snake Game PRD

## 1. 게임 정체성

- 게임 이름: Snake Game
- 장르: 모바일 전용 아케이드 / 성장형 스네이크 게임
- 플랫폼: 모바일 웹 우선
- 입력 방식: 터치 조작만 지원
- 핵심 경험: 넓은 필드에서 부드럽게 방향 전환하는 뱀을 터치로 조작하며, 사과 같은 아이템을 먹어 레벨을 올리고 몸의 길이와 크기를 키우는 게임

## 2. 제품 목표

플레이어는 넓은 게임 필드 안에서 뱀을 조작해 아이템을 찾아 먹습니다.
아이템을 먹을 때마다 레벨이 올라가며, 레벨은 뱀의 길이와 크기를 결정합니다.
게임은 모바일 터치 조작만으로 플레이 가능해야 하며, 큰 필드 안에서 탐색과 방향 전환의 재미를 제공해야 합니다.

## 3. 핵심 규칙

1. 뱀은 터치 위치를 기준으로 연속 각도 방향으로 이동할 수 있습니다.
2. 게임 필드에는 사과 같은 성장 아이템이 놓여 있습니다.
3. 뱀이 아이템을 먹으면 레벨이 올라갑니다.
4. 레벨은 뱀의 길이와 몸 크기를 결정합니다.
5. 게임 필드는 크게 구성합니다.
6. 게임 필드의 초기 크기는 50x50칸입니다.
7. 조작은 터치만 지원합니다. 개발/브라우저 확인용 마우스 포인터 입력은 터치 입력과 같은 방식으로 처리합니다.
8. 자기 몸 또는 필드 벽과 충돌해도 게임오버되지 않습니다.
9. 자기 몸 또는 필드 벽과 충돌하면 일반적인 빛의 반사각처럼 충돌 각도에 따라 방향이 반사됩니다.
10. 반사된 다음 위치가 다시 자기 몸에 가깝거나 벽 밖이면 안전한 후보 각도 중 하나를 랜덤으로 지정해 반복 튕김을 방지합니다.
11. 게임은 정해진 승리 조건 없이 최대한 오래 성장하는 무한 플레이를 목표로 합니다.

## 4. 이동 설계

뱀은 초기 8방향 격자 이동에서 연속 좌표 기반 이동으로 개선합니다.
플레이어가 화면을 누르거나 드래그하면 화면 중앙의 뱀 머리 기준으로 터치 지점까지의 각도를 목표 방향으로 삼습니다.
실제 진행 방향은 목표 방향으로 즉시 꺾이지 않고 초당 회전 제한 안에서 부드럽게 따라갑니다.

이동 기준:

- 좌표계는 50x50 필드 안의 연속 좌표를 사용합니다.
- 기본 속도는 초당 6칸입니다.
- 모든 각도에서 같은 속도로 이동하도록 방향 벡터를 정규화합니다.
- 기본 회전 속도는 초당 약 261도입니다.
- 몸통은 머리가 지나간 경로를 일정 거리 간격으로 샘플링해 따라오게 합니다.

자기 몸이나 필드 벽과 충돌하면 게임오버 대신 반사각 규칙에 따라 방향을 바꿉니다.
벽 충돌은 벽의 법선 기준으로 진행 벡터를 반사합니다.
몸통 충돌은 충돌한 몸통 위치에서 머리까지의 방향을 법선처럼 사용해 진행 벡터를 반사합니다.

## 5. 필드 크기

필드는 플레이어가 충분히 탐색한다고 느낄 정도로 크게 설정합니다.

기준:

- 뱀이 1칸 이동하는 시간: 0.2초
- 한 축을 끝까지 이동하는 목표 시간: 약 10초
- 필요한 칸 수: 10초 / 0.2초 = 50칸

초기 필드 크기:

- 가로: 50칸
- 세로: 50칸

초기 구현에서는 기존 100x100칸에서 절반으로 낮춘 50x50칸을 사용합니다.
화면은 항상 뱀 머리를 중앙에 고정합니다.
카메라 또는 뷰포트는 뱀 머리를 기준으로 움직이며, 전체 필드를 축소해서 보여주지 않습니다.

## 6. 레벨과 성장

레벨은 아이템을 먹을 때 증가합니다.

초기 규칙:

- 시작 레벨: 1
- 아이템 1개 획득 시 레벨 +1
- 레벨이 오르면 뱀의 길이가 증가합니다.
- 레벨이 오르면 뱀의 몸 크기도 매 레벨 증가합니다.

성장 공식:

- 뱀 길이: 레벨과 동일한 칸 수
- 뱀 크기: 매 레벨 증가

## 7. 아이템

초기 아이템은 사과입니다.

사과 규칙:

- 필드의 빈 칸에 생성됩니다.
- 뱀 머리 중심이 사과 중심에서 3칸 이내에 들어오면 사과를 먹습니다.
- 사과를 먹으면 레벨이 1 증가합니다.
- 사과를 먹은 뒤 새 사과가 다른 빈 칸에 생성됩니다.

판정 단위:

- 사과 먹기 판정은 `칸(cell)` 단위 반경으로 지정합니다.
- 현재 값은 `사과 판정 반경 3칸`입니다.
- 앞으로 판정을 조정할 때는 "사과 판정 반경 N칸"처럼 전달합니다.

향후 확장 가능한 아이템:

- 추가 점수 아이템
- 일시적 속도 변화 아이템
- 크기 변화 아이템
- 위험 아이템

## 8. 모바일 터치 조작

이 게임은 모바일 전용이므로 터치 조작을 기본 입력으로 설계합니다.

조작 방식은 화면 방향 터치와 드래그입니다.
뱀 머리는 항상 화면 정중앙에 위치합니다.
플레이어가 화면의 특정 지점을 누르면, 뱀은 중앙에 있는 자기 머리 기준으로 해당 지점의 각도를 목표 방향으로 삼습니다.
플레이어가 누른 채 드래그하면 목표 방향이 계속 갱신됩니다.
중앙 근처에는 작은 데드존을 두어 손가락이 머리 근처에 있을 때 불필요한 방향 흔들림을 줄입니다.

가상 조이스틱과 스와이프 입력은 별도 UI로 만들지 않습니다.

## 9. 게임 상태

초기 상태:

- 시작 전
- 플레이 중
- 일시정지
- 재시작

초기 화면:

- 시작 화면
- 게임 화면
- 일시정지 화면
- 재시작 확인 화면

게임 화면에 표시할 정보:

- 현재 레벨
- 현재 길이
- 획득한 아이템 수
- 필요하면 현재 방향

## 10. 실패 조건

초기 버전은 무한 성장 플레이를 목표로 하므로, 벽 충돌과 자기 몸 충돌은 게임오버가 아닙니다.
기본 규칙상 일반적인 게임오버 상태는 사용하지 않습니다.

충돌 규칙:

- 자기 몸에 부딪히면 충돌 각도에 따라 일반적인 빛의 반사각처럼 방향을 바꿉니다.
- 필드 벽에 부딪혀도 자기 몸 충돌과 같은 룰을 적용합니다.
- 필드 벽에 부딪히면 벽의 축에 맞춰 방향을 반사합니다.
- 반사된 다음 위치가 자기 몸에 가깝거나 다시 벽 밖이면 안전한 후보 각도 중 하나를 랜덤으로 선택합니다.
- 충돌은 즉시 방향 전환 효과를 만들지만 게임을 종료하지 않습니다.
- 장애물 충돌 규칙은 아직 정의하지 않습니다. 초기 버전에는 장애물을 넣지 않습니다.

## 11. 레벨 디자인

현재 요구사항에서는 레벨이 스테이지가 아니라 성장 단계의 의미에 가깝습니다.

정의:

- 레벨 1: 시작 상태
- 레벨 2 이상: 사과를 먹을 때마다 증가하는 성장 레벨
- 레벨이 오를수록 뱀 길이와 크기가 매번 증가
- 뱀 길이는 현재 레벨과 같은 칸 수
- 승리 조건은 없으며, 플레이는 무한 성장 방식으로 계속됩니다.

추후 스테이지형 레벨을 도입할 경우 아래 항목을 정의합니다.

- 레벨별 필드 크기
- 레벨별 아이템 수
- 레벨별 장애물 배치
- 레벨별 이동 속도
- 레벨별 승리 조건

## 12. 시각 스타일

초기 방향:

- 모바일에서 명확히 보이는 단순한 색상
- 뱀, 사과, 필드 배경의 대비를 크게 유지
- 터치 조작 영역이 게임 플레이를 방해하지 않도록 최소한의 UI 사용

구체적인 색상, 분위기, 그래픽 스타일은 아직 미정입니다.

## 13. 기술 요구사항

- 모바일 웹에서 실행되어야 합니다.
- 터치 이벤트를 기본 입력으로 사용합니다.
- 뱀 머리는 항상 화면 중앙에 고정되어야 합니다.
- 화면 방향 터치/드래그로 연속 각도 이동을 제어해야 합니다.
- 게임 상태 확인을 위해 `window.render_game_to_text()` 같은 텍스트 상태 훅을 제공합니다.
- Playwright 또는 유사 도구로 기본 게임 루프를 검증할 수 있어야 합니다.
- 개발 지침은 `.agents/skills/game-dev` 문서를 따릅니다.

## 13.1 Expo Android WebView 실행

Android 모바일 기기 실행은 Expo 앱 안에서 현재 Phaser 웹 게임을 `react-native-webview`로 감싸는 방식을 사용합니다.

실행 구조:

- `App.tsx`는 전체 화면 `WebView`를 렌더링합니다.
- `scripts/bundle-game-html.mjs`는 `vendor/phaser.min.js`, `styles.css`, `main.js`를 하나의 HTML 문자열로 묶습니다.
- 생성된 `src/generated/gameHtml.ts`를 WebView의 `source.html`로 전달합니다.
- Phaser는 CDN이 아니라 로컬 `vendor/phaser.min.js`를 사용해 Android에서도 네트워크 없이 실행될 수 있게 합니다.

개발 명령:

- `npm run game:bundle`: WebView용 HTML 번들을 생성합니다.
- `npm run expo:start`: 번들을 생성한 뒤 Expo 개발 서버를 시작합니다.
- `npm run android`: 번들을 생성한 뒤 Android 기기 또는 에뮬레이터에 실행합니다.

설치형 Android 테스트:

- EAS development build는 `eas.json`의 `development` profile을 사용합니다.
- Android 실기기에 직접 설치하려면 `.aab`가 아니라 `.apk`가 필요하므로 development/preview profile은 `android.buildType: "apk"`를 사용합니다.
- Expo SDK 55 기준 로컬 Node.js는 `>=20.19.4`가 필요합니다.

## 14. 첫 번째 마일스톤

최소 플레이 가능 버전에서 반드시 구현할 것:

- 모바일 화면에 게임 필드 렌더링
- 뱀 생성
- 연속 각도 기반 이동
- 터치 기반 방향 변경
- 사과 생성
- 사과 획득
- 레벨 증가
- 레벨에 따른 길이 증가
- 레벨에 따른 몸 크기 증가
- 자기 몸 충돌 시 반사각 기반 방향 전환
- 필드 벽 충돌 시 반사각 기반 방향 전환
- 뱀 머리 중앙 고정 카메라
- 기본 게임 상태 텍스트 훅

## 15. 확정된 보완 답변

이 섹션은 초기 보완 질문에 대한 확정 답변을 기록합니다.

1. 자기 몸 충돌은 게임오버가 아니라 충돌 각도 기준으로 뱀의 방향을 반사하는 역할을 합니다.
2. 터치 조작은 화면 방향 터치/드래그 방식으로 합니다.
3. 뱀은 항상 화면 정중앙에 위치합니다.
4. 뱀의 몸 크기는 매 레벨 커집니다.
5. 뱀의 길이는 레벨과 같은 길이입니다.
6. 필드 초기값은 50x50칸입니다.
7. 화면은 항상 뱀 머리를 가운데 고정합니다.
8. 게임은 무한 성장 플레이입니다.
9. 필드 벽에 부딪힐 때도 자기 몸 충돌과 같은 룰을 적용하되, 충돌 각도에 따른 반사 방향을 사용합니다.
10. 반사 방향의 다음 위치에 꼬리가 있으면 안전한 후보 각도 중 하나를 랜덤으로 지정합니다.

## 16. 추가로 정하면 좋은 항목

아래 항목은 구현 중 또는 다음 설계 단계에서 정하면 됩니다.

1. 매 레벨마다 몸 크기가 얼마나 커질지에 대한 정확한 수치
2. 몸 크기가 커질 때 필드 칸 크기도 함께 커지는지, 시각적 크기만 커지는지
3. 사과 생성 위치가 화면 밖이어도 되는지, 아니면 플레이어 주변 일정 거리 안에 생성할지
4. 뱀의 최대 레벨 또는 성능 보호를 위한 상한이 필요한지
5. 벽에 튕길 때 이펙트, 사운드, 짧은 무적 시간 같은 피드백을 넣을지
